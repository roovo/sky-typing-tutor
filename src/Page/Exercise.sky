module Page.Exercise exposing (Model, Msg, init, view)

import Exercise exposing (Exercise)
import Sky.Core.Prelude exposing (..)
import Std.Ui as Ui
import Std.Ui exposing (Element)
import Std.Ui.Background as Background
import Std.Ui.Font as Font

--
-- MODEL
--
type alias Model =
    { exercise : Exercise }


init : String -> String -> Model
init filename exerciseText =
    { exercise = Exercise.init filename exerciseText }

--
-- UPDATE
--
type Msg
    = KeyDown Int


--
-- VIEW
--
view : (Msg -> parentMsg) -> Model -> Element parentMsg
view toMsg model =
    Ui.column
        [ Ui.padding 24
        , Ui.spacing 16
        , Background.color (Ui.rgb 18 22 38)
        , Font.color (Ui.rgb 220 220 230)
        ]
        [ heading model, exerciseBody model ]


heading model =
    Ui.el
        [ Font.color (Ui.rgb 142 200 168) ]
        (Ui.text <| Exercise.title model.exercise)


exerciseBody model =
    Ui.el
        [ Font.color (Ui.rgb 142 200 168) ]
        (Ui.text <| Exercise.title model.exercise)
