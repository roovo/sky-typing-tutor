module ExerciseParser exposing (toSteps)

import Step exposing (Step(..))


toSteps : String -> List Step
toSteps string =
    let
        typeables = String.toList string |> List.map Typeable
    in
        typeables ++ [ Step.initEnd ]
