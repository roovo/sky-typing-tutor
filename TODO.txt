- show a summary on exercise completion
- show progress in the footer (lines and/or characters remaining - completed when finished)
- persist attempts (text file or sqlite database)
- have some way of tracking with keyboard is being used
- have a way to adjust the window size

Desirable sky changes:
- Debug.log
- Cmd.map
- allow function definitions in let blocks
- allow Page.Exercise to be used as Page.Exercise without having to alias it as PageExercise
- point free notation
- port Parser.elm (as it relies on Kernel modules)
- 'sky test' will by default run all tests in the tests directory
- "describe" is added to test for grouping tests (needs to be in the output too)
- toFloat (having to do String.fromInt n |> String.toFloat |> Maybe.withDefault 0.0)
- Tui - able to determine how many rows high it is

sky bugs:
- timeTaken events = events |> List.map .timeTaken |> List.foldl (\n acc -> n + acc) 0
  The .timeTaken field accessor is causing the runtime type mismatch. This is a codegen issue with field accessors used as first-class functions in List.map. 

