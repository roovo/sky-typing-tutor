- do some other analysis: e.g. character most mistyped, slowest to type, isues with specific character combinations
  - should "fromEventList returns a list of CharStrokes when backspacing past the mistake" list the mistakes
    under 'b' rather than 'a'
- add an attempts page
- did I make this slower when I move to windowing being done on Typeables instead of Steps (commit 0b35871)
- factor out database stuff (look at new sky database stuff too)
- track lines or typeable lines during exercise
- do I want to change the position the list on Page.Exercises starts scrolling? (make it near the bottom)
- have a way to adjust the window size
- generate drills based on performance
- config file to hold targetWpm, language mappings, and default terminal height and have some way of tracking which keyboard is being used


Desirable sky changes:
- Debug.log
- allow function definitions in let blocks
- allow Page.Exercise to be used as Page.Exercise without having to alias it as PageExercise
- port Parser.elm (as it relies on Kernel modules)
- 'sky test' by default could/should run all tests in the tests directory
- "describe" added to test for grouping tests (needs to be in the output too)
- toFloat (having to do String.fromInt n |> String.toFloat |> Maybe.withDefault 0.0)
- Tui - able to determine how many rows high the terminal is (and track if it changes)
- fuzzer support in sky test
- looks line function definitionn in let blocks are supported, but there is no checking of them
